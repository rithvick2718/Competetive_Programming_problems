#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int n,m;
    cin>>n,m; //so I know that I can have at max m points
    // we cannot transfer points between n,m
    int x=0;
    int r;
    for(int i=0; i<n; i++)
    {
        cin>>r;
    }
    //THIS IS DP COMEBACK WHEN YOU ARE READY!!
}