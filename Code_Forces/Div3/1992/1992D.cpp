#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int t; //testcase
    cin>>t;
    int n; // length of river
    int m; // distance we can jump (can be used infinetly)
    int k; // meters he can survive in water
    string p;
    while(t--)
    {
        cin>>n>>m>>k;
    }
}