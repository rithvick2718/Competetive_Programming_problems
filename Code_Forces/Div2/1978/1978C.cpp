#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int t; //testcases
    cin>>t;
    while(t--)
    {
        //code for each test case
        int n;
        long long int k;
        cin>>n>>k;
        vector<int> permutation(n+1); //+1 for better indexing
        for(int i=0; i<=n; i++)
        {
            permutation[i]=i;
        }//so get the identity permutation
        
    }
}